# 绑定所有 IP（Docker 内部通常不需要改，但若需外部访问可设为 0.0.0.0）
bind 0.0.0.0

# 启用守护进程（Docker 中不需要，保持默认 no）
daemonize no

# 设置密码（取消注释并修改）
requirepass 123456

# RDB 持久化（默认已开启）
save 900 1
save 300 10
save 60 10000

# AOF 持久化（可选）
appendonly yes